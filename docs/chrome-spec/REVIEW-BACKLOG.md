# Review backlog — one-tube chrome

The adversarial review (6 finder lenses, refute-biased verification, 15
agents) confirmed 7 defects — all fixed in the working tree — refuted 2,
and surfaced 53 findings that were **reported without verification** (the
verify fan-out was capped). Each row below is a claim, not a fact: verify
against the file before acting on it.

- **[high·settings]** Sidebar texture toggle textures the case, not the panels, and its strength slider mostly repaints the case near-black
  - Entries at 8279 ('Sidebar texture — Concrete grain on the side panels', class-toggle default true) and 8284 ('Sidebar texture strength — How visible the sidebar paper-grain texture is'). The side panels are transparent glass in the new chrome (6376-6404 sets background-color: transparent on all sidebar leaf/view surfaces). The toggle's CSS lands on .app-container (924-932), which is now the machin…
- **[high·tooling]** Exemption substrings match the whole selector line, so dual-theme and content-mentioning chrome rules are never hue-checked
  - PAPER/CONTENT/CHASSIS checks (lines 98-100) run selector.includes() on the entire selector text. Consequences verified in theme.css: (1) every ':is(.theme-dark, .theme-light) ...' rule contains the substring 'theme-light' and is PAPER-exempt even though it styles DARK chrome too — ~100 such rules exist (theme.css 986-1450 metadata/property chrome, 5796 .cm-highlight, 7575 area). A dark-chrome viol…
- **[high·tooling]** Multi-line selector lists are attributed to the last line only; earlier selectors lose their check (or gain a bogus exemption)
  - Line 93: selector = line.split('{')[0].trim() fires only on the line containing '{'. For 'a,\nb {' the whole block is judged as 'b', and the 'a,' line itself is attributed to the PREVIOUS rule. Verified in-file blocks where the brace line is exempt while earlier selector lines are not: theme.css 858 ('body.theme-dark::after,' + 'body.theme-light::after {' — the dark atmosphere overlay is PAPER-exe…
- **[high·tooling]** Lint only sees 6-digit hex literals — rgb()/hsl()/#rrggbbaa/#rgb/named/oklch/color() are format-invisible
  - Line 111 matches /#[0-9a-f]{6}\b/ only. An 8-digit hex (#rrggbbaa) never matches because \b cannot fall between two hex digits; 3/4-digit hex, rgb(), hsl(), oklch(), color-mix() between literals, and named colors are all unchecked. theme.css already carries chrome colors in rgba() form (e.g. the sheen tint rgba(196, 224, 248, ...) at 6209-6210) — in-hue today, but a violation restated in any non-6…
- **[high·tooling]** Light mode has zero automated coverage: lint PAPER-exempts it and the test suite never renders it
  - Both harness bodies hard-code class="theme-dark ..." (test/harness/index.html:13, editor.html:23) and 'theme-light' appears nowhere in either harness; test-editing.mjs never toggles it. So the greyscale law check (Math.abs(act-rest) > 40, tube tests, sheen/frame assertions) runs only in the dark direction, while lint-phosphor exempts every 'theme-light'/'after-l-'/'after-d-' rule as PAPER (lint-ph…
- **[high·tooling]** CHASSIS exemption list contradicts the design model: status bar and vault chip are glass, but lint treats them as case
  - CHASSIS (line 16-19) includes 'status-bar-item' and 'sidedock-vault-profile', but theme.css's own section header (~6117) states 'Everything else, status bar and vault switcher included, is drawn on the tube', and the nameplate rules (theme.css 7231-7241) paint the vault chip as reverse video on the glass (--after-norm plate, --after-tube ink, --after-prot legend). Any selector containing those sub…
- **[medium·laws]** Light-mode chrome palette is literals, not phosphor derivations — switching tubes does not change light-mode ink
  - In body.theme-light the four intensities ARE derived (lines 6271-6273, color-mix with var(--after-phosphor)), proving derivation was achievable — but everything else in the same block bypasses it: line 6284 `--after-tube: #e8edf1`; lines 6294-6296 frames `#8ea2b2` / `#3d5d76` / `#c0ccd6`; lines 6297-6304 `--after-c1` through `--after-c6` as literals (#16222c, #1e2a35, #33424f, #3f5568, #000d16, #5…
- **[medium·laws]** Literal blue-white 'emitted light' values in dark mode that dodge the lint
  - Two dark-mode values are self-described emission yet are hue literals that never follow the coating: (1) line 6160 `--after-recess-lip: inset 0 -1px 0 rgba(190, 224, 244, 0.12)` — its own comment says "emitted light on the foot", i.e. phosphor light falling on the countersink wall, but it stays blue-white on an amber or green tube; it escapes lint-phosphor.mjs twice over, via the CHASSIS_TOKENS na…
- **[medium·laws]** Two rasters over the same note glyphs, with the section contradicting itself about which is intended
  - The workspace scan overlay (line 6418, .workspace::before, background-image: var(--after-tube-grid), multiply, inset: 0) covers the ENTIRE glass including every note pane. The editor's own scanline overlays are still active with no after-cards guard: lines 3352-3360, `body.theme-dark .markdown-source-view.mod-cm6 .cm-editor::after` and `body.theme-dark .markdown-reading-view::after` painting `back…
- **[medium·laws]** The status bar is 'drawn on the glass' but escapes the raster, the sheen, and the glyph glow
  - §STATUS (line ~7108) fixes .status-bar over the tube's reserved bottom line with `position: fixed; z-index: 20`. Its DOM position is outside .workspace (the comment says so), and .workspace carries `isolation: isolate` (line 6342), so the scan (z-index 40) and sheen (z-index 41) resolve inside the workspace's stacking context and paint BELOW the root-level fixed status bar. Result: the status run …
- **[low·laws]** Unfocused-group active tab is a fill outside law 4's three exemptions
  - Lines 6600-6604: `.workspace-tabs:not(.mod-stacked):not(:has(.workspace-tab-header.mod-active)) .workspace-tab-header.is-active { background-color: var(--after-prot); }`. Law 4 permits exactly three fills — reverse video, the 7% wash, a knockout — and law 3 defines reverse video's plate as NORMAL intensity ("REVERSE — ink and ground swapped: the plate is NORMAL", restated at line 6231). A protecte…
- **[low·laws]** Fabricated 'VAULT' label contradicts the section's own no-invented-text rule
  - Line 7221: `.workspace-sidedock-vault-profile::before { content: "VAULT"; }` injects invented English-only UI text. Lines 6971-6975 in the same section refuse to synthesize a sidebar pane title from [data-type] on exactly this ground: "Inventing a label from [data-type] would be fabricated, English-only text, so it is not done." The two rules apply opposite principles; the forbidden-moves table ("…
- **[low·laws]** Model acceptance items with no implementation in the chrome section: bracketed sidebar toggles and the tube refresh bar
  - (1) DESIGN-MODEL.md acceptance line 156: ".sidebar-toggle-button left and right both present and bracketed." No rule between lines 6084-7360 targets .sidebar-toggle-button; the only mentions in theme.css are mobile styling (line 2157) and the hide toggles (lines 3767-3768). The bracketed-control grammar (lines ~7011-7045) covers only .nav-action-button and .nav-buttons-container .clickable-icon. (…
- **[low·laws]** Stale seven-step / two-face prose inside the §TYPE preamble contradicts the rules beneath it
  - The §TYPE header (line 6664) is still titled "THE CHROME RAMP, C1 TO C7" and its preamble (lines 6668-6670) states "Face carries kind: 3270 for anything the machine says about itself, Courier Prime for anything that came out of the vault" — a two-typeface hierarchy the model's forbidden-moves table bans ("A second typeface for hierarchy. One character generator."). The C2 rule directly below (line…
- **[medium·cascade]** --after-chassis now defined unguarded on body — case grey leaks into the mobile and cards-off status bar via the line-2740 fallback
  - Line 2740 (pre-existing): `.status-bar { background-color: var(--after-chassis, var(--after-bg-alt)); border-top: 1px solid var(--after-border); ... }` — written when --after-chassis was undefined (confirmed: `git show HEAD:theme.css` has no `--after-chassis:` definition, so the --after-bg-alt fallback was live). The one-tube token block defines --after-chassis on bare `body` (line 6129 dark #2628…
- **[medium·cascade]** Case hardware (rail gradient, keycaps, chassis app-container) escapes the soft-look and cards-off guards
  - The guard-chain convention (`body.after-cards:not(.is-mobile):not(.after-pill-chrome):not(.after-glass-cards):not(.after-wallpaper)`) is dropped on the hardware rules: line 6301 `body.after-cards:not(.is-mobile):not(.after-wallpaper) .app-container { background-color: var(--after-chassis) }` does not exclude pill-chrome or glass-cards, so those looks get a flat #26282e desk instead of the profile …
- **[medium·cascade]** after-float ('Heavier card float') is now a dead control in its documented configuration
  - Lines 2496-2499: `body.after-cards.after-float .workspace-split .workspace-leaf-content { margin: 6px 6px calc(var(--after-card-gap) + 16px) 6px; box-shadow: var(--after-glass-tl), var(--after-glass-br), 0 22px 44px -20px ..., ... }` — specificity (0,4,1). The one-tube reset at 6365-6374 (`body.after-cards:not(.is-mobile):not(.after-pill-chrome):not(.after-glass-cards):not(.after-wallpaper) :is(.m…
- **[medium·cascade]** Body-level :not(.after-tree-stamp) guards strip the terminal row grammar from every pane, not just the file explorer
  - Six one-tube rules carry `:not(.after-tree-stamp)` on the body compound (lines 6799, 6812, 6872, 6878, 6882, 6888 — C2 vault-name ink, folder ink, transparent rows, wash hover, reverse-video selection, reverse ink). But after-tree-stamp (lines 1548-1630) only styles `.workspace-leaf-content[data-type="file-explorer"]`. Enabling the 'registry look' therefore removes the C2 face/ink, transparent row…
- **[medium·cascade]** Stacked tabs (.mod-stacked) still paint the old card/accent grammar on the glass — half-migrated
  - Every new tab/frame/reverse rule is scoped `.workspace-tabs:not(.mod-stacked)`, but no one-tube treatment for stacked mode exists, so the old rules still apply inside the tube: line 2267-2284 spine `background-color: var(--after-editor-bg); background-image: var(--after-grain); box-shadow: -2px 0 8px -5px var(--after-shadow); border-radius: var(--after-card-radius)`; line 2285-2287 active spine `b…
- **[medium·cascade]** View-header action icons have no hover state in one-tube mode — stateless high-specificity rule beats every :hover
  - Lines 7040-7046: `body.after-cards:not(...)x4 :is(.mod-root, .mod-left-split, .mod-right-split) .workspace-tabs:not(.mod-stacked) .view-header :is(.clickable-icon, .view-action) { background-color: transparent; color: var(--after-prot); }` — specificity ~(0,10,1), matches in the hover state too, and the section defines no :hover companion ('Colour only' comment, but no hover colour is ever declare…
- **[medium·cascade]** Viewport vignette and accent halo overlays paint over the case, rail, keycaps and status strip, double-vignetting the tube
  - Line 900-907: `.app-container::after` — position:fixed, inset:0, z-index 9997, `radial-gradient(... rgba(0,0,0, var(--after-vignette-amount, 0.5)) ...)` — the 0.5 fallback means it is ON by default. It sits above the fixed rail (z 30) and status strip (z 20) and covers the whole window, so the machined case, keycaps and status text now darken toward the corners like a faceplate — impossible on har…
- **[low·cascade]** Hide-status-bar / Focus mode leave a 26px dead strip reserved at the tube's foot
  - Lines 3760-3763: `body.after-focus .status-bar { display: none }` and `body.after-hide-status-bar .status-bar { display: none }` still hide the (now fixed) bar, but the glass unconditionally reserves the status line: the .workspace rule at 6320-6348 keeps `padding: var(--after-glass-pad) var(--after-glass-pad) var(--after-status-h) var(--after-glass-pad)` (26px bottom) regardless of those body cla…
- **[low·cascade]** Fullscreen CRT 'chrome recede' list predates the drawn-frame grammar — it dims half the new chrome
  - Lines 3487-3494: `body.is-fullscreen.after-fullscreen-crt .workspace-ribbon, .status-bar, .workspace-tab-header-container, .workspace-split.mod-left-split, .workspace-split.mod-right-split { opacity: 0.2 }` (default true). In the one-tube chrome the pane's identity moved into elements this list does not cover: the drawn frame (border on .workspace-tab-container), the ┤ TITLE ├ header rule (.view-h…
- **[medium·safety]** Other in-pane editable fields inherit the chrome glow: search input, file-explorer rename, canvas node label
  - Same mechanism as the Live Preview leak (glow inherited from `.workspace`, theme.css:6421-6423), different editable surfaces, all inside the glass: (a) the search pane's text input — `.search-input-container input` is styled at theme.css:1470-1481 (background-color/border/color only, no text-shadow), so typed search text glows; (b) inline file rename — the C2 row rule at theme.css:6810-6824 (`.tre…
- **[medium·safety]** after-poweron animation re-anchors the fixed ribbon: transform/filter on .workspace makes it the containing block for position:fixed
  - The new rule at theme.css:7109-7116 makes `.workspace-ribbon.mod-left` `position: fixed; left:0; top:0; bottom:0; z-index:30`. The ribbon is a DOM child of `.workspace` (modelled the same way in test/harness/index.html:16-19). The pre-existing power-on toggle animates `.workspace` itself: theme.css:3941-3943 applies `animation: after-appstart ...`, and the keyframes at theme.css:3838-3844 animate …
- **[low·safety]** The editing-safety test harness cannot detect the Live Preview glow leak: editor.html omits .cm-sizer / .inline-title / .metadata-container
  - The Live Preview harness (test/harness/editor.html:30-45) nests `.cm-content` directly under `.cm-scroller` with no `.cm-sizer`, no `.cm-contentContainer`, no `.inline-title`, and no `.metadata-container` — the exact structures through which the chrome glow reaches editable text in real Obsidian (findings above). Properties are modelled only in the reading-view page (test/harness/index.html:140-16…
- **[medium·dom]** Glass margin reserves the rail unconditionally; Obsidian's own --ribbon-width is never read or written
  - `.workspace` margin-left is `calc(var(--after-rail-w) + var(--after-case-reveal))` (6343–6346) with `--after-rail-w: calc(var(--after-icon-box) + var(--after-s2) * 2 + var(--after-s3))` = 58px (6178), and the ribbon is sized by the same calc directly on width/min-width (5977–5978). Nothing ties the margin to the ribbon's actual presence or rendered width: (1) if the ribbon is hidden — Hider/Minima…
- **[medium·dom]** Fixed status bar has no overflow handling: long plugin item sets escape the 26px line
  - The §STATUS rule (7067–7083) makes `.status-bar` `position: fixed; right: calc(...); bottom: calc(...); height: var(--after-status-h); max-width: 60vw` with `padding: 0` and no `overflow` declaration and no flex-wrap control. Real vaults run sync/word-count/language-tool/tasks/clock plugins that together easily exceed 60vw, and single items (e.g. LanguageTool status, long file paths from various p…
- **[medium·dom]** Frameless-window geometry ignored: rail at top:0 under macOS traffic lights, and frame=custom titlebar overlapped
  - Three interacting rules assume the viewport top belongs to the theme: (1) `body:not(.is-mobile) .workspace-ribbon.mod-left { ... padding: 0; ... }` (5975–5984) resets ribbon padding with a shorthand, discarding whatever padding-top Obsidian applies for the hidden-frame (default) macOS window controls; (2) the rail is then `position: fixed; left: 0; top: 0; bottom: 0` (7121–7130), and `.side-dock-a…
- **[low·dom]** Tab drag ghost falls outside every tab rule and renders in the pre-rework style
  - All tab grammar — reverse-video plate, 31px height, C3 label face/colour, divider ticks — is scoped under `.workspace-tabs:not(.mod-stacked)` inside `:is(.mod-root, .mod-left-split, .mod-right-split)` (e.g. 6540–6560, 6584–6592, 6795–6810). When the user drags a tab, Obsidian appends a `.workspace-tab-header` clone to a body-level drag-ghost container that has no `.workspace-tabs` ancestor, so non…
- **[low·dom]** overflow: visible + halos on active elements can spill outside a collapsing sidebar split
  - The pane-stripping rule forces `overflow: visible` on every `.workspace-tabs` (6379) and gives active tabs `box-shadow: var(--after-halo)` (6584–6592) and active tree rows the same halo (6903–6907); the split-border removal rule (6367–6371) also strips the sidebar splits of their own borders. During Obsidian's animated sidebar collapse/expand, `.mod-left-split` transitions its inline width — if th…
- **[low·dom]** Rail-width variable chain verified: resolves at body scope (no defect)
  - Direct answer to the margin-resolution question: `--after-s1..s5` and `--after-icon-box` are declared in a `body { }` block (5959–5968), `--after-rail-w` in the chrome `body { }` block (6123, declaration at 6178), and `--after-gutter`'s dependency `--after-card-gap` in the `.theme-dark, .theme-light, ...` block (declaration at 303) — all on the body element, so the `.workspace` margin at 6343–6346…
- **[medium·settings]** Effects mode Low/Medium claims exclude the heaviest layers the theme now has
  - Entry at 7812: 'Low = base styling only · Medium = + textures & glow'. The override block (4165-4176) predates the rework: it kills the editor overlays, body::before grain, body::after glow, and .app-container::before halo, but never touches the new chrome compositing — the full-workspace multiply raster and screen sheen pseudo-layers (6414-6427), the tube vignette/sheen background layers and spil…
- **[medium·settings]** Card shadow sliders (darkness/blur/spread) are inert in the shipped default configuration
  - Entries at 8426 (after-card-shadow), plus after-card-shadow-blur and after-card-shadow-spread ('How dark/soft ... the drop-shadow under each card'). Sole consumer is the box-shadow at 2464-2465 on .workspace-split .workspace-leaf-content. Under the default chrome the guard-chain rule at 6376-6394 (specificity ~(0,7,1) vs (0,2,1)) sets box-shadow: none on the same elements, and the chrome doctrine …
- **[medium·settings]** after-float (Heavier card float) loses every declaration to the guard-chain rule
  - Entry at 8481 ('Deeper, larger drop shadow so panes float higher off the desk (needs Cards layout on)'). Its rule at 2496-2499 (body.after-cards.after-float .workspace-split .workspace-leaf-content — specificity (0,4,1)) declares only margin and box-shadow, both of which are also declared by the higher-specificity tube rule at 6376-6394 ((0,7,1), margin: 0 / box-shadow: none). Since .after-float i…
- **[medium·settings]** after-flat-edges 'keep the drop shadow' now deletes the only shadow (and silently kills Screen glow)
  - Entry at 8491 ('Drop the inset glass bevel on cards and panes but keep the drop shadow'). The rule at 7495-7507 zeroes --after-glass-tl/br and --after-recess-top/lip (the bevel, as described) but also replaces --after-cutout with '0 0 0 2px var(--after-bezel)', dropping the aperture's occlusion shadow AND var(--after-spill). In the dark tube there is no drop shadow to keep (6166-6168); in light mo…
- **[medium·settings]** after-card-radius no longer rounds any pane; pane silhouette is a fixed 16px it does not drive
  - Entry at 8418 ('Card rounding — Corner radius, in pixels') under the 'Cards & layout' heading. In the default chrome the panes it implies are forced border-radius: 0 (6376-6394) and the visible silhouette is --after-tube-radius: 16px, a literal at 6175 the slider does not feed. Remaining live consumers are peripheral: Bases cards view (1270), empty-state action list (1917), the stacked-tab spine (…
- **[medium·settings]** Single-id tube sliders clobber the theme-light recalibration when adjusted
  - after-tube-glow (8164) and after-tube-scan (8180) are single variable-number-slider ids, but body.theme-light re-tunes the same custom properties in CSS (6285 --after-tube-glow: 0.12; 6286 --after-tube-scan: 0.1). Style Settings writes one mode-blind value via its own body.css-settings-manager rule, which outranks body.theme-light (equal specificity, later in document), so any user adjustment forc…
- **[medium·settings]** Starting-points import snippets reference eight setting ids that do not exist
  - after-creator-note (7801-7804) offers two paste-in Style Settings imports whose keys include afterimage@@ai-profile, ai-body-font, ai-glyph-raster, ai-prose-raster, ai-display-raster, ai-bloom, ai-ghost, ai-rgb-split. No setting in the @settings block has an ai- prefixed id (grep 'id: ai-' matches nothing; the real ids are after-profile, after-body-font, after-display-raster, after-prose-raster, a…
- **[medium·settings]** Harnesses render a hybrid settings state no user sees; the new no-animation assertion is vacuous
  - Both harness bodies now add after-cards (index.html:13, editor.html:23) on the stated ground that it 'is ON by default in Style Settings' — but every other default-ON class (after-anim, after-block-caret, after-panel-grain, after-rail, after-ruler, after-poweron, after-bootflash, after-crossfade, after-glow-ease, after-menu-fade, after-tooltip-type, after-scan-anim, after-glow-pulse, after-fullscr…
- **[low·settings]** after-tube-vignette description still speaks per-pane
  - Entry at 8172: 'Corner falloff across each pane'. The variable's only consumer is --after-tube-vig (6205-6207), applied once as a background layer on .workspace (6357-6358) — one vignette across the single glass. No per-pane vignette exists anywhere. The daylight claim ('stays on ... where the glow does not') is still true (no theme-light override; --after-screen-on stays 1).
- **[low·settings]** after-tube-scan claims the raster covers everything the pane draws; the status line escapes it
  - Entry at 8180: 'a multiply layer sitting OVER everything the pane draws'. The layer is .workspace::before (6414-6420, z-index 40 inside the workspace's isolated stacking context). The status bar — which the chrome section defines as 'a drawn line on the glass' (7052-7075) — is position: fixed, z-index 20, a sibling OUTSIDE .workspace, so it paints above the whole tube stack and takes no raster or …
- **[low·settings]** after-card-gap repurposed: floored, clamped, and no cards involved
  - Entry at 8410 ('Card gap — Space between cards, in pixels', min 0). Alive, but in the default chrome it feeds --after-gutter = max(4px, calc(var(--after-card-gap) - 2px)) (6184) — spacing between drawn frames on the glass — so the slider's 0 actually produces 4px, every value is shifted by -2px, and below 900px the gutter is pinned to 6px regardless (7323-7324). Residual legacy consumers (2283, 23…
- **[low·settings]** after-editor-only-cards has no visible effect in the default chrome
  - Entry at 8506 ('Only the editor floats as a card — strip the card border, rounding and shadow from the sidebars'). Its rule at 3030-3037 strips exactly what the tube guard rule (6376-6394) already strips from sidebar leaf-contents, and in the tube the editor does not float as a card either, so toggling it changes nothing a user can see in the default state; its stated purpose is unachievable there…
- **[low·settings]** Two stacked vignette systems with indistinguishable descriptions
  - Atmosphere 'Vignette' (8292, --after-vignette-amount, default 0.5) still works: fixed .app-container::after at z-index 9997 (901-912). It now paints on top of the tube's own 'Faceplate vignette' (8172, also default 0.5) over the same corners, and also darkens the machined case — which the chrome doctrine says never takes picture effects (6126-6129, housing is 'deliberately NOT derived from the pal…
- **[low·settings]** Shadow strength description lists surfaces that no longer take shadows
  - Entry at 8271: 'Depth of shadows on callouts, properties, cards, the editor and side panels'. In the default chrome the editor and side panels have box-shadow: none (6376-6394) and no cards exist; --after-shadow (312-313) remains live for callouts, menus, popovers and the soft looks, so the slider works but three of the five named targets are gone.
- **[low·settings]** Test comment exempts CRT artifacts that are not in the CSS
  - Lines 163-167 justify the animation-scope exemption with 'the refresh bar and the mains flicker are CRT artifacts, not decoration', citing DESIGN-MODEL.md, which itself asserts the drifting refresh bar 'stays' (docs/chrome-spec/DESIGN-MODEL.md:86) yet lists it as an unbuilt checklist item (:153). theme.css declares no animation on .workspace::before/::after at all, so the exemption exempts nothing…
- **[low·settings]** Body preset description lists Typewriter twice for a four-option select
  - after-body-font (7858-7876): the description enumerates five entries — 'Typewriter' appears both first and last with near-identical text — against four options. A leftover from re-ordering; cosmetic but the info-text misdescribes the control.
- **[medium·tooling]** Tube-switch test probes six ink properties only — no plates, no --after-frame, no selection, no vault chip
  - The probe (tubes section, ~line 200) reads color on five elements plus borderLeftColor of the first '.mod-root .workspace-tab-container'. Because the harness root tab carries mod-active (index.html:101), that container matches the ':has(.workspace-tab-header.mod-active)' rule (theme.css 6483-6488) and the probe measures --after-frame-hi — plain --after-frame (resting pane frames, the band hairline…
- **[medium·tooling]** A comment line containing '{' poisons the lint's selector tracking and can exempt the rest of a real chrome block
  - Line 93 runs before the comment skip at line 101, and only lines STARTING with /* are skipped — so any comment line containing '{' (including interior lines of a multi-line comment) becomes the tracked selector until the next real '{'. If that comment text contains an exempt substring, subsequent declarations in the enclosing block are exempted. Verified in-file: theme.css 4320 (a comment quoting …
- **[medium·tooling]** Z-order assertions are absolute thresholds, not relative comparisons
  - The IME check asserts compZ >= 6 against the raster's hard-coded z of 5 (theme.css 4873, 7440) instead of reading the raster's actual z-index and requiring compZ > rasterZ — raising the raster to 7 keeps the test green while composition sinks beneath it. Likewise 'tube raster sits OVER the content' asserts scan.z >= 10 (actual: 40, theme.css chrome section z-index 40/41) but never asserts sheen.z …
- **[low·tooling]** chromeAnim stillness check silently drops selectors that stop matching
  - The 'no chrome ELEMENT animates' probe builds its element list with .map(s => document.querySelector(s)).filter(Boolean) — a renamed or removed harness class silently shrinks coverage to the surviving elements and the check still passes. (All eight selectors currently exist in test/harness/index.html — verified — and, by contrast, the tube probe's 'missing' sentinel and the sidebar 'present and me…
- **[low·tooling]** Minor lint scoping defects: --after-wallpaper-* matches the chassis-token regex; after-profile selectors without a tubes entry are fully exempt; phosphor decl regex accepts formats hexToHsl cannot parse
  - (1) CHASSIS_TOKENS /--after-(chassis|wall|...)[\w-]*/ (line 43): 'wall' prefix-collides with the --after-wallpaper-url/-dim/-snippet tokens (theme.css 2505-2517), exempting wallpaper declarations as 'case material'. (2) Line 107: a selector matching after-profile-(\w+) with no tubes[] entry returns unchecked — this covers 'custom' by design, but also 'body.theme-light.after-profile-cobalt' (theme.…
